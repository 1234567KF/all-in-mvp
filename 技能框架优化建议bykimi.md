# all-in-mvp 白皮书优化建议（by kimi）

> 审视维度：理论覆盖度、执行准确性、稳定性
> 迭代轮次：3 轮递进优化

---

## 第一轮：结构性缺陷与边界盲区

### 1.1 理论覆盖度缺陷

#### 缺失 1：迭代循环机制（非一次性瀑布）
白皮书呈现的是线性四阶段流程（Stage1→2→3→4→交付），但 MVP 开发本质是**螺旋迭代**——交付后必然有反馈回流到 Stage1。当前文档在"交付"节点戛然而止，未定义：
- 用户反馈如何回流到 PRD 修订？
- 小迭代是否可跳过 Stage2 直接进 Stage3？
- 多轮 MVP 迭代之间如何复用已有产物？

**建议**：在第十章后增加「十一、迭代演进规则」，定义：
- 快速迭代路径：PRD 局部变更 → 仅重跑受影响模块的 Stage2 子集 → Stage3 增量开发
- 产物复用规则：Schema 不变更时直接复用锁定版 spec，仅更新变更模块的 `<module>.md`
- 版本标记机制：产物文件名纳入迭代版本号（如 `spec-v2.md`）

#### 缺失 2：Agent 失效与降级策略
白皮书假设所有 Agent 都能正常工作，但未定义：
- Agent 输出质量不达标时的降级路径（如业务专家拆模块出现循环依赖）
- Agent 上下文窗口耗尽时的断点续传机制
- 多 Agent 并发时的资源争抢（如多个 Agent 同时写入同一文件）

**建议**：在第六章增加「6.4 Agent 异常处理原则」：
- 质量不达标：触发 Review Agent 介入 → 仍不达标则降级为人类接管
- 上下文耗尽：按文件边界拆分任务，新 Agent 实例读取产物文件续接
- 写入冲突：文件锁机制（`WIP-<module>` 标记文件）

#### 缺失 3：量化度量体系
全文无 KPI/度量指标，无法判断流程健康度：
- 各阶段预期耗时基准？
- Agent 产出质量评分标准？
- 集成测试通过率阈值？

**建议**：在角色映射表（第五章）增加「预期耗时」和「质量门禁」列：

| 角色 | 预期耗时 | 质量门禁 |
|------|---------|---------|
| 产品经理 | 1-2h | PRD MECE 检查通过 |
| 架构专家 | 30-60min | Schema 无冗余字段，接口覆盖率 100% |
| 后端开发（单模块） | 2-4h | 单元测试覆盖率 ≥80%，Red-Green-Refactor 全通过 |

### 1.2 执行准确性缺陷

#### 模糊点 1：「最多 3 个 Agent 并行」的硬上限依据不足
3.1 节和 3.2 节均规定"最多 3 个 Agent"，但未说明：
- 为什么是 3 而非 2 或 4？
- 超过 3 个模块时的排队策略是什么？
- 3 个 Agent 是硬性技术约束还是经验值？

**建议**：补充硬上限的决策依据：
> 上限 3 基于以下约束：① 上下文窗口成本——每个 Agent 独立会话消耗独立上下文配额；② 协调复杂度——Pipeline Coordinator 维护 3 个并行状态已接近人类可追踪上限；③ 模块粒度——MVP 阶段单业务域通常可拆为 3-6 个模块，分两批执行不会显著增加总耗时。

#### 模糊点 2：Code Review 触发后的流转未闭环
3.1 节提到「Review 不通过 → 打回修改 → 重新 TDD 循环」，但未定义：
- Review Agent 的产出物格式是什么？
- Backend Agent 如何读取 Review 意见？
- 最多可打回几次？无限循环怎么办？

**建议**：在 3.1 节补充 Code Review 流转细节：
```
Review Agent 产出：`<module>.review.md`
  ├── 问题分类：[阻塞]/[建议]/[风格]
  ├── 阻塞问题清单（必须修复）
  ├── 建议清单（可选修复）
  └── 修复指引（引用 spec.md 具体条款）

Backend Agent 读取 review 文件 → 修复 → 删除 `DONE` 标记 → 重新 TDD → 再次标记 `DONE` → Review Agent 复评

打回上限：同一模块最多 3 轮 Review，第 3 轮仍不通过 → 人类介入
```

#### 模糊点 3：Mock 与真实 API 切换机制
4.2 节提到「前端切换 Mock → 真实后端 API」，但未说明：
- 切换时机（所有模块完成后？还是逐模块切换？）
- 切换方式（环境变量？配置文件？）
- 切换后发现不匹配的回退策略？

**建议**：在 4.2 节补充切换机制：
> 切换策略：按模块逐个切换。某模块后端 DONE 且通过 Code Review 后，前端对应页面即可切换。切换方式：前端配置文件 `api.config.ts` 中按模块映射 baseURL（`user: mock` → `user: real`）。回退策略：切换后发现问题 → 标记该模块为 `BLOCKED` → 回退到 Mock → Debug Agent 修复 → 再次切换。

### 1.3 稳定性缺陷

#### 脆弱点 1：grill-with-docs 循环无超时保护
2.3 节提到「建议上限 3 轮」，但 3 轮后仍未达成一致时直接降级为人类决策——这意味着最坏情况下循环可能无限拖延。

**建议**：增加超时机制：
> 每轮审查预期耗时 10-15 分钟。若 30 分钟内未完成一轮，或 3 轮后仍有未决问题 → 自动触发降级：grill-with-docs 按「最简实现原则」输出推荐方案 → 人类 15 分钟内确认 → 超时未确认则自动采用推荐方案。

#### 脆弱点 2：Pipeline Coordinator 单点故障
Stage3 完全依赖 Pipeline Coordinator 一个 Agent，若其调度逻辑出错（如遗漏模块、错误排序），整个 Stage3 将偏离轨道。

**建议**：增加 Coordinator 自检机制：
> 每轮分配后，Coordinator 必须执行「三检」：① 已分配集合 + 未分配集合 == 模块全集；② 已分配模块的依赖是否全部 DONE；③ 本轮分配数 ≤ 空闲 Agent 数。任一检查失败 → 写入 `SCHEDULER_ERROR.md` → 人类介入。

#### 脆弱点 3：文件状态标记的竞态条件
9.3 节定义了 `DONE`/`BLOCKED` 标记文件，但未处理并发写入场景（如 Backend Agent 刚写完 `DONE`，Coordinator 同时扫描）。

**建议**：补充原子性保证：
> 标记文件写入采用「先写临时文件再重命名」策略：`DONE.tmp` → 原子重命名为 `DONE`。Coordinator 只识别不以 `.tmp` 结尾的标记文件。

---

## 第二轮：深度逻辑矛盾与一致性修复

### 2.1 理论覆盖度：前后矛盾

#### 矛盾 1：Stage2 的串行定位 vs 并行收益论述
2.7 节对比表中声称"业务专家纯推理几分钟 + ↺ 循环校验几分钟，串行代价极小"——但实际项目中：
- PRD 复杂时业务专家拆模块往往需要 30 分钟以上
- grill-with-docs 审查一轮通常需要 15-20 分钟，两轮即 30-40 分钟
- 总延迟可达 1 小时，对于"几分钟"的描述过于乐观

**建议**：修正时间估算，增加「复杂度分级」：
> 时间估算（按 PRD 复杂度）：
> - 简单 PRD（<10 个功能点）：业务专家 10-15min，↺ 循环 1 轮 10min
> - 中等 PRD（10-30 个功能点）：业务专家 20-30min，↺ 循环 2 轮 30min
> - 复杂 PRD（>30 个功能点）：业务专家 40-60min，↺ 循环 2-3 轮 40-60min

#### 矛盾 2：③b-1「最多 2 个 Agent 并行」的依据与后端「最多 3 个」不一致
2.5 节规定单模块测试最多 2 个 Agent，但 3.1 节后端开发最多 3 个——测试并行度低于开发并行度，可能导致测试成为瓶颈。

**建议**：统一并行度逻辑或明确差异原因：
> 测试并行度（2）< 开发并行度（3）的原因：测试用例编写需要更长的上下文保持（每个测试文件需理解完整模块契约），Agent 上下文消耗更快。若资源允许，可将 ③b-1 上限提升至 3，与后端对齐。

#### 矛盾 3：「Code Review 由独立 Review Agent 执行」与角色映射表冲突
3.1 节提到 Code Review 由独立 Review Agent 执行，但第五章角色映射表中「Code Review」行的并行度为「按需触发」——未明确 Review Agent 是独立 spawn 还是复用已有 Agent。

**建议**：明确 Review Agent 的 spawn 策略：
> Review Agent 为按需独立 spawn，不占用 Backend/Frontend Agent 配额。每触发一次 Review，独立 spawn 一个 Review Agent 实例，Review 完成后销毁。避免 Review 与开发争抢 Agent 资源。

### 2.2 执行准确性：遗漏关键步骤

#### 遗漏 1：Stage4 缺少「产物归档」步骤
4.1-4.4 节描述合并、联调、测试、修复，但交付前缺少产物归档——产物清单（第八章）中的文件散落在各阶段目录，未定义最终交付包的目录结构。

**建议**：在 Stage4 增加「4.5 产物归档」：
```
delivery/
  ├── docs/
  │   ├── PRD.md
  │   ├── spec.md
  │   ├── api-contract.yaml
  │   └── task.md
  ├── backend/
  │   ├── src/
  │   └── tests/
  ├── frontend/
  │   ├── src/
  │   └── tests/
  └── integration-tests/
      ├── modules/
      └── scenarios/
```

#### 遗漏 2：Schema 锁定后 Drizzle Migration 的执行时机未定义
2.1 节产出 `schema.sql` 或 Drizzle Schema，但未说明 Migration 何时执行（Stage2？Stage3 每个模块？Stage4 合并后？）。

**建议**：明确 Migration 时机：
> Drizzle Migration 执行时机：Stage4 后端合并完成后统一执行。Stage3 各模块开发时使用 `db:push`（开发模式，直接同步 Schema 到本地 SQLite），不执行正式 Migration。Stage4 合并后执行 `drizzle-kit generate` + `drizzle-kit migrate`，生成正式 Migration 文件纳入版本控制。

#### 遗漏 3：前端 Mock 服务的生命周期未定义
2.4 节产出 Mock 服务，但未说明：
- Mock 服务是独立进程还是前端代码的一部分？
- Stage4 联调后 Mock 服务是否保留？
- Mock 数据如何维护（与真实数据同步）？

**建议**：补充 Mock 生命周期：
> Mock 服务独立运行（推荐 MSW 或 json-server），前端通过环境变量切换 baseURL。Stage4 联调完成后，Mock 服务保留在 `mocks/` 目录作为前端独立开发的基础设施，不删除。Mock 数据与真实数据保持结构一致——Schema 变更时需同步更新 Mock 数据模板。

### 2.3 稳定性：未定义的回退路径

#### 缺失回退 1：PRD 锁定后变更的完整流程
第 29 行提到「PRD 变更需走变更评审」，但未定义变更评审的流程和触发条件。

**建议**：在第六章增加「6.5 PRD 变更控制流程」：
```
变更请求 → 评估影响范围 → 判断变更级别：
  ├── 微小变更（文案、字段名）：直接更新 PRD，通知相关 Agent 刷新上下文
  ├── 中等变更（新增字段、调整规则）：重跑 Stage2 ①② + ↺ 循环，受影响的 <module>.md 更新
  └── 重大变更（新增模块、调整架构）：从 Stage2 重新执行，Stage3 已完成的受影响模块标记为 REBUILD
```

#### 缺失回退 2：Stage3 中 Backend Agent 失败的处理
3.1 节未定义 Backend Agent 开发失败（如无法实现某接口、测试始终无法通过）时的处理。

**建议**：增加失败处理：
> Backend Agent 开发失败判定：同一模块 TDD 循环超过 5 轮仍无法 Green，或 Code Review 打回超过 3 轮。处理：标记模块为 `BLOCKED` + 失败原因 → Pipeline Coordinator 将该模块降级为「人类开发」→ 其他无依赖模块继续并行。

#### 缺失回退 3：集成测试大面积失败的应急方案
4.3 节未定义集成测试通过率低于阈值时的处理。

**建议**：增加应急方案：
> 集成测试通过率阈值：Happy Path 100% 通过，Exception Path ≥80% 通过。低于阈值：停止交付 → Debug Agent 按优先级修复（阻塞问题 > 功能缺陷 > 边界偏差）→ 修复后重新执行全量集成测试。

---

## 第三轮：语义精确性与可执行性增强

### 3.1 理论覆盖度：术语与概念精确化

#### 问题 1：「Agent」一词的多义性
全篇混用「Agent」指代：① AI 会话实例（如 Backend-1 Agent）；② 角色定义（如"产品经理 Agent"）；③ 执行单元（如"spawn 一个 Review Agent"）。三义混用导致理解歧义。

**建议**：统一术语：
- **Agent 实例**：具体的 AI 会话（如 Backend-1 是一个 Agent 实例）
- **Agent 角色**：角色定义（如"产品经理"是一个 Agent 角色）
- **Agent 任务**：分配给实例的具体工作单元

#### 问题 2：「锁定版」的状态转移未显式定义
2.2 节提到产出后标记为【初版】，经 ↺ 循环后升级为【锁定版】——但「锁定」是逻辑状态还是文件标记？如何防止已锁定的文件被误修改？

**建议**：显式定义锁定机制：
> 【锁定版】实现方式：文件名后缀标记（`spec.locked.md`）+ 文件头声明（`<!-- STATUS: LOCKED -->`）。锁定后任何修改需先解除锁定（人类授权 → 重命名为 `.draft.md` → 修改 → 重新走 ↺ 循环 → 恢复 `.locked.md`）。

#### 问题 3：「模块」的粒度未量化
全篇使用「模块」作为拆分单元，但未定义模块的合理粒度——一个模块多大？包含多少接口？多少张表？

**建议**：增加模块粒度指南：
> 模块粒度标准：
> - 接口数量：5-15 个 API 端点
> - 数据库表：1-3 张核心表（不含关联表）
> - 代码行数：预估 200-800 行（含测试）
> - 开发耗时：2-4 小时（单 Backend Agent）
> 超过上限 → 拆分为子模块；低于下限 → 合并到相邻模块。

### 3.2 执行准确性：产出物格式标准化

#### 标准化 1：各产出物文件的头信息模板
当前文档只列出产出物名称，未定义文件头必须包含的元信息。

**建议**：为每个产出物定义标准头：

```markdown
<!-- spec.md 标准头 -->
# Spec: <项目名>
- 版本: v1.0.locked
- 基于 PRD: PRD.md (v1.0.locked)
- 生成时间: 2026-05-21
- 负责 Agent: 架构专家-①
- 变更历史:
  - v1.0.draft: 初版产出
  - v1.0.locked: ↺ 循环通过后锁定
```

#### 标准化 2：审查报告的标准格式
2.3 节提到「写入审查报告文件」，但未定义报告格式。

**建议**：定义 `review-report.md` 标准格式：
```markdown
# 审查报告: <轮次>
- 审查时间: <timestamp>
- 审查对象: ① spec.md / ② trace.md
- 审查结果: [通过] / [不通过]

## 发现的问题
| 序号 | 严重程度 | 审查维度 | 问题描述 | 引用 PRD 条款 | 修正建议 |
|------|---------|---------|---------|-------------|---------|
| 1 | 阻塞 | 需求覆盖 | trace_code 字段类型不符 | PRD §4.2 | 拆分为三个字段 |

## 修正验证
- [ ] 问题 1 已修正
- [ ] 术语一致性已复核
```

#### 标准化 3：DONE/BLOCKED 标记文件的内容格式
9.3 节定义了标记文件，但未定义文件内容。

**建议**：定义标记文件标准内容：
```markdown
<!-- DONE 标记文件示例: src/modules/user/DONE -->
# 模块完成标记
- 模块: user
- 完成时间: 2026-05-21T14:30:00Z
- 负责 Agent: Backend-1
- 单元测试: 12/12 通过
- Code Review: 通过（review-user.md v1）
- 依赖模块: 无
```

### 3.3 稳定性：防呆与自检机制

#### 机制 1：每个 Stage 的准入检查清单
当前文档只在 ↺ 循环处定义了门禁，其他阶段无准入检查。

**建议**：为每个 Stage 定义准入检查：

| Stage | 准入检查项 | 检查方式 |
|-------|-----------|---------|
| Stage1 | PRD 包含全部 9 个章节 | 自动检查章节标题 |
| Stage2 ① | PRD 已锁定 | 检查 `PRD.locked.md` 存在 |
| Stage2 ② | ① 产出物已锁定 | 检查 `spec.locked.md` 等存在 |
| Stage2 ↺ | ①② 产出物均为初版 | 检查 `.draft.md` 后缀 |
| Stage2 ③a/b | ↺ 循环通过 | 检查 `review-report.final.md` 存在且结果为通过 |
| Stage3 | task.md + 全部 <module>.md 已锁定 | 扫描 `.locked.md` 数量 == 模块数 |
| Stage4 | 全部模块 DONE | 扫描 DONE 标记数量 == 模块数 |

#### 机制 2：产物依赖追踪图
各产出物之间存在隐式依赖（如 `<module>.md` 依赖 `spec.md`），但未显式追踪。

**建议**：增加产物依赖声明：
> 每个产出物文件头增加 `依赖:` 字段，形成可解析的依赖图。例如 `trace.md` 的头：
> ```
> 依赖: PRD.md, spec.locked.md, schema.locked.sql
> ```
> Coordinator 启动时解析全部依赖图，发现缺失依赖 → 阻止进入下一阶段。

#### 机制 3：Agent 上下文刷新机制
长流程中 Agent 可能因上下文过长而丢失早期信息。

**建议**：定义上下文刷新规则：
> Agent 上下文窗口使用超过 70% 时，触发刷新：Agent 将当前工作摘要写入 `<agent>.context.md` → 销毁旧实例 → spawn 新实例 → 新实例读取摘要 + 相关锁定版产物 → 继续工作。摘要格式：
> ```
> # Agent 上下文摘要
> - 角色: Backend-1
> - 当前模块: user
> - 已完成: 路由定义、Service 实现、5/12 测试通过
> - 待完成: 7 个测试用例、Code Review
> - 关键决策: 使用 bcrypt 而非 argon2（性能考量）
> ```

---

## 附录：优化建议优先级汇总

| 优先级 | 轮次 | 优化项 | 影响范围 |
|--------|------|--------|---------|
| P0 | R1 | 增加迭代演进规则（第十一章） | 全局 |
| P0 | R1 | 增加 Agent 异常处理原则（6.4） | Stage2-4 |
| P0 | R1 | 增加量化度量体系 | 全局 |
| P0 | R2 | 定义 PRD 变更控制流程（6.5） | Stage1-2 |
| P0 | R2 | 定义 Stage4 产物归档（4.5） | Stage4 |
| P1 | R1 | 明确 3 Agent 硬上限依据 | Stage3 |
| P1 | R1 | 闭环 Code Review 流转 | Stage3 |
| P1 | R1 | 补充 Mock 切换机制 | Stage4 |
| P1 | R2 | 修正时间估算，增加复杂度分级 | Stage2 |
| P1 | R2 | 明确 Review Agent spawn 策略 | Stage3 |
| P1 | R2 | 定义 Drizzle Migration 时机 | Stage2-4 |
| P1 | R3 | 统一 Agent 术语 | 全局 |
| P1 | R3 | 显式定义锁定机制 | Stage2 |
| P1 | R3 | 增加模块粒度指南 | Stage2 |
| P2 | R1 | 增加 grill-with-docs 超时保护 | Stage2 |
| P2 | R1 | 增加 Coordinator 自检机制 | Stage3 |
| P2 | R1 | 文件标记原子性保证 | Stage3-4 |
| P2 | R2 | 统一并行度逻辑 | Stage2-3 |
| P2 | R2 | 增加 Stage3 失败处理 | Stage3 |
| P2 | R2 | 增加集成测试应急方案 | Stage4 |
| P2 | R3 | 产出物头信息模板 | 全局 |
| P2 | R3 | 审查报告标准格式 | Stage2 |
| P2 | R3 | DONE/BLOCKED 标记内容格式 | Stage3-4 |
| P2 | R3 | Stage 准入检查清单 | 全局 |
| P2 | R3 | 产物依赖追踪图 | 全局 |
| P2 | R3 | Agent 上下文刷新机制 | Stage3 |

---

*本文档为 all-in-mvp 白皮书优化建议，供迭代参考。*
