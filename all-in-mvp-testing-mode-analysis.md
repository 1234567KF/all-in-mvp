# all-in-mvp 测试模式分析厘清

> 编写时间：2026-06-24
> 目标读者：需要理解 Pipeline 测试分层设计的人员

---

## 一、当前三层测试架构

all-in-mvp Pipeline 中的测试分为三个层次，各自的运行方式不同：

```
                    ┌─────────────────────────────────────┐
                    │      L1-L3 Vitest API 集成测试       │
                    │  不启动浏览器，直接调后端路由         │
                    │  Hono app.request() + SQLite in-memory│
                    └──────────────┬──────────────────────┘
                                   │
           ┌───────────────────────┼───────────────────────┐
           ▼                       ▼                       ▼
┌────────────────────┐ ┌────────────────────┐ ┌────────────────────┐
│  L4 有头浏览器测试  │ │  L5 无头浏览器测试  │ │  ad-hoc Browser测试 │
│  Playwright headed  │ │  Playwright headless│ │  Browser MCP 手动   │
│  本地人工验证        │ │  CI 自动回归        │ │  临场调试/抽查      │
│  slowMo:100         │ │  全自动无干扰       │ │  当前对话中使用     │
└────────────────────┘ └────────────────────┘ └────────────────────┘
```

| 层级 | 工具 | 浏览器 | 运行场景 | 执行者 |
|:---:|:----|:-----:|:---------|:------|
| L1-L3 | Vitest | ❌ 无 | 每次提交/PR | CI / 本地 |
| L4 | Playwright headed | ✅ 有头 | 本地开发验证 | 开发者 |
| L5 | Playwright headless | ✅ 无头 | CI 流水线 | CI |
| ad-hoc | Browser MCP Agent | ✅ 有头 | 人工抽查/调试 | 你 / AI |

---

## 二、结论：无需替换，但需补充分工说明

### 2.1 不需要替换的理由

| 论点 | 结论 |
|:----|:-----|
| **L4 vs L5 是互补关系，不是替代关系** | 保持现状 |
| **L4 有头 + slowMo 是为人工肉眼观察设计** | 合理，各有用处 |
| **L5 无头已用于 CI 加速** | 性能收益已落地 |
| **L1-L3 Vitest 测试不涉及浏览器** | 已是最优模式 |

### 2.2 需要补充厘清的方面

以下 4 点是当前技能定义中**缺乏明确表述或容易误解**的地方，建议补充说明。

---

## 三、需要补充的厘清点

### 厘清 1：Browser MCP Agent 的角色定位

**现状**：`kf-mvp` 系列技能中没有一个明确说明"Browser MCP 子 Agent"和"Playwright L4/L5"之间的关系。

**建议补充**：

> **Browser MCP Agent ≠ Playwright 测试**
>
> | 维度 | Playwright (L4/L5) | Browser MCP Agent |
> |:----|:------------------|:------------------|
> | 用途 | 自动化回归测试 | 人工抽查/调试/演示 |
> | 运行方式 | 脚本驱动，全自动 | AI 实时操作交互 |
> | 截图用途 | 自动像素对比基线 | 人工复盘查阅 |
> | 速度 | 无头 15min / 有头 33min（500用例） | 较慢（逐步操作+截图） |
> | 是否纳入管道 | ✅ 是 | ❌ 否，仅临场使用 |
>
> Pipeline 的测试质量不依赖 Browser MCP Agent，它只是一个辅助调试工具。

### 厘清 2：截图的真实用途

**现状**：代码注释中写的是"截图对比（有头模式下人工可查看）"，但没有分析截图到底给谁看。

**建议补充**：

> **截图的两类消费者**
>
> ```
> 截图 ──┬── Playwright toHaveScreenshot() ── 自动化像素级对比（CI 无头也可执行）
>        │                                      结果：PASS / VISUAL_REGRESSION
>        │
>        └── 人工肉眼审阅 ── 截图保存到 screenshots/ 目录
>                                           用途：复盘、验收、Bug 报告附件
> ```
>
> AI 不消费截图像素内容，也不依赖视觉识别来判定测试通过与否。所有可判定断言都通过以下方式完成：
>
> | 断言方式 | 示例 | AI 是否可验证 |
> |:---------|:-----|:-------------:|
> | DOM 元素存在性 | `expect(btn).toBeVisible()` | ✅ 是 |
> | Computed Style | `toHaveCSS('color', 'rgb(59,130,246)')` | ✅ 是 |
> | 响应数据结构 | `expect(res.data.name).toBe('...')` | ✅ 是 |
> | 像素对比 | `toHaveScreenshot()` | ✅ Playwright 自动对比，不依赖 AI |
> | 截图文件内容 | `screenshots/*.png` | ❌ AI 不消费 |

### 厘清 3：视觉自验证协议的分工边界

**现状**：`kf-mvp-frontend-dev` 中定义了 3 道视觉防线，但 Pipeline 其他地方没有引用它。

**建议补充**：

> **视觉验证的 3 道防线在 Pipeline 中的归属**
>
> ```
> Pipeline 门禁
> ├── L1-L3 Vitest：无需视觉验证
> ├── L4 有头 Playwright：
> │   ├── 防线 1（Computed Style 断言）→ 测试代码中硬编码
> │   └── 防线 2（像素快照）→ 生成基线 + 人工审阅
> └── L5 无头 Playwright：
>     ├── 防线 1（Computed Style 断言）→ 正常运行
>     ├── 防线 2（像素快照）→ 与基线自动对比
>     └── 防线 3（A11y Tree 快照）→ 结构回归检测
> ```
>
> **关键原则**：无论有头还是无头，防线的**可判定性**不变。防线 1 和防线 3 在任何模式下都产生 AI 可验证的确定性数据。防线 2（像素快照）由 Playwright 自动对比，结果也是确定的 PASS/FAIL。

### 厘清 4：有头与无头的已知差异（来自真实项目经验）

**现状**：`kf-mvp-backend-tdd` 中记录了 3 种差异情况，但分散在注释中，没有集中呈现。

**建议补充为集中表格**：

> **有头/无头差异速查（来自实际项目经验）**
>
> | # | 现象 | 根因 | 排查方向 |
> |:-:|:----|:-----|:---------|
> | 1 | L4 ✅ L5 ❌ | 无头下字体/滚动条渲染差异 | 用 `waitForSelector` 替代固定 timeout |
> | 2 | L4 ❌ L5 ✅ | 有头环境下 CORS/CSP 策略拦截 | 检查浏览器 console 中的安全错误 |
> | 3 | L4 ✅ L5 ✅ 但人工发现 Bug | 测试断言不够严格 | 加强断言粒度（检查具体值而非 "not null"） |
> | 4 | 截图像素 diff 很大 | 不同 OS/字体渲染差异 | 设置 `maxDiffPixels: 100` 合理容差 |
>
> **建议 Pipeline 门禁增加一项检查**：
> ```markdown
> - [ ] 有头/无头一致性：L4 和 L5 结果一致
> ```

---

## 四、测试模式决策树

将上述所有分析浓缩为一张决策参考：

```
你需要做什么类型的验证？
│
├── 功能逻辑正确性（API 返回数据对不对）
│   └── → 用 L1-L3 Vitest（不启动浏览器，最快）
│
├── 页面交互完整性（按钮能不能点，表单能不能提交）
│   └── → 用 L5 Playwright 无头（够用，速度快）
│
├── 视觉样式正确性（颜色、间距、字体对不对）
│   ├── → 用 Computed Style 断言（AI 可验证）
│   └── → 用 toHaveScreenshot（Playwright 自动对比）
│
├── 人工验收/演示
│   └── → 用 L4 Playwright 有头 或 Browser MCP Agent
│
└── 排查失败的测试
    └── → 用 L4 Playwright 有头（肉眼观察操作过程）
```

**结论**：当前的有头/无头分层设计是合理的，不需要做替换。只需补充上述厘清说明，让读者清楚每层的设计意图和适用边界。
