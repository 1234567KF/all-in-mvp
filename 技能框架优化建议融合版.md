# MVP技能框架优化建议 融合版

> **生成时间**：2026-05-21
> **融合来源**：`技能框架优化建议bykimi.md` + `byminimax.md` + `bypro.md` + `flash.md`
> **对比基准**：`MVP白皮书最终融合版.md` v2.2（~1350行，14章+零章）
> **方法论**：逐一交叉比对四份优化建议 vs v2.2最终版，剔除已吸收项，合并剩余有效建议

---

## 一、交叉比对：四份建议在 v2.2 中的吸收状态

### 1.1 已完全吸收（无需再提）

以下建议已在 v2.2 最终版中得到体现，不再纳入本次融合建议：

| 来源 | 原建议 | v2.2对应位置 |
|------|--------|-------------|
| kimi | 迭代循环机制（非一次性瀑布） | §0 迭代模型与生命周期 |
| kimi | Agent失效与降级策略 | §6.4 Agent异常处理 / §11 |
| kimi | 量化度量体系（耗时+门禁） | §5 角色映射表 / §12 |
| kimi | 3 Agent硬上限依据 | §0.7 并行度上限依据 |
| kimi | Code Review流转闭环 | §3.1 Review流转+打回上限 |
| kimi | Mock与真实API切换机制 | §4.2 切换策略 |
| kimi | 时间估算复杂度分级 | §2.8 时间估算表 |
| kimi | Review Agent spawn策略 | §3.1 Review Agent独立spawn |
| kimi | Drizzle Migration执行时机 | §4.1 Migration执行时机 |
| kimi | PRD变更控制流程 | §0.4 PRD变更分级控制 |
| kimi | Stage4产物归档 | §4.5 产物归档 |
| kimi | 锁定机制显式定义 | §2 锁定版实现 |
| kimi | 模块粒度量化 | §2.2 模块粒度标准 |
| kimi | 审查报告标准格式 | §2.3 审查报告标准格式 |
| kimi | DONE标记内容格式 | §9.3 DONE标记文件标准内容 |
| kimi | grill超时保护 | §2.3 超时保护 |
| kimi | Coordinator自检机制 | §3.0 自检三检 |
| kimi | 文件标记原子性 | §9.3 原子性保证 |
| kimi | 并行度逻辑统一 | §2.5 测试并行度说明 |
| kimi | Stage3失败处理 | §3.1 Backend开发失败处理 |
| kimi | 集成测试通过率阈值 | §4.3 通过率阈值 |
| kimi | 产出物头信息模板 | §2.1 产出物标准头 |
| kimi | Agent上下文刷新 | §6.4 上下文摘要格式 |
| kimi | Mock生命周期 | §2.4 Mock生命周期 |
| kimi | 产物复用规则 | §0.5 产物复用规则 |
| kimi | 版本标记机制 | §0.6 版本标记机制 |
| kimi | 已知局限+适用边界声明 | 文档头部 |
| bypro | 迭代模型（全量/增量双模式） | §0.1 两种运行模式 |
| bypro | 回退/回滚协议 | §0.3 回退协议 |
| bypro | 非功能需求演进时间线 | §2.1 非功能需求演进时间线 |
| bypro | Agent失败恢复模型 | §11 Agent失败恢复 |
| bypro | 人类决策框架完整化 | §6.3 人类决策点完整清单 |
| bypro | 跨Agent知识共享协议 | §9.5 知识共享协议 |
| bypro | PRD质量门禁 | §1 PRD质量门禁 |
| bypro | 前端测试方法论（三层） | §3.2 前端测试层级 |
| bypro | 并行度上限理论依据 | §0.7 并行度上限依据 |
| bypro | ③c测试用例静态审查 | §2.7 ③c |
| bypro | Mock一致性验证前置 | §3.2 Mock持续验证 |
| bypro | 依赖图自动校验 | §3.0 启动时依赖图自动校验 |
| bypro | 前端页面拆分策略 | §3.2 前端页面分配算法 |
| bypro | Code Review时机明确化（6阶段表） | §3.1 CR量化标准 |
| bypro | 事件驱动测试补充触发条件 | §3.3 事件驱动触发机制 |
| bypro | Coordinator推送模式 | §3.0 Push模式+Slot状态 |
| bypro | Coordinator高可用（状态持久化） | §9.4 状态持久化 |
| bypro | 人类离线自动降级 | §6.3 离线自动策略 |
| bypro | Stage4 Coordinator | §4.0 Stage4 Coordinator |
| bypro | 风险矩阵扩充（12项+概率维度） | §10 风险与应对 |
| bypro | BLOCKED处理闭环 | §11.3 BLOCKED标记生命周期 |
| bypro | 信息集中化（白皮书+SKILL.md分层） | §4.2引用说明 |
| bypro | 原子表组标注 | §2.1 @atomic_group |
| byminimax | CR触发量化标准 | §3.1 CR量化标准表 |
| byminimax | ↺循环终止强制规则 | §2.3 强制降级策略 |
| byminimax | 并发写入冲突检测（WIP锁） | §6.4 写入冲突 |
| byminimax | Bug修复循环终止条件 | §4.4 循环终止条件表 |
| byminimax | Agent异常恢复机制 | §11 Agent失败恢复 |
| byminimax | Skill映射关系表 | §5 Skill映射表 |
| byminimax | Agent心跳机制 | §11.2 心跳机制 |
| flash | 部分内容已通过bypro/kimi路径间接吸收 | — |

### 1.2 统计

| 来源 | 总建议项（估算） | 已吸收 | 未吸收 | 吸收率 |
|------|----------------|--------|--------|--------|
| bykimi | ~35 | ~33 | ~2 | 94% |
| byminimax | ~15 | ~12 | ~3 | 80% |
| bypro | ~37 | ~33 | ~4 | 89% |
| flash | ~27 | ~10 | ~17 | 37% |
| **合计** | **~114** | **~88** | **~26** | **77%** |

> flash 吸收率较低的原因：其建议大量聚焦于 **Skill/Agent实现层的操作细节**（如Agent配置文件修改、代码级检查清单），而非白皮书流程框架层。白皮书 v2.2 定位于执行标准，实现细节应由配套 SKILL.md 承载。

---

## 二、未吸收建议分类与优先级评估

按主题将剩余 ~26 项建议归为 7 大类，每类评估优先级：

| 类别 | 项数 | 优先级 | 理由 |
|------|------|--------|------|
| A. 模块合并与测试执行精细化 | 3 | 🔴 P0 | 直接影响Stage4交付质量 |
| B. ↺循环判定锚点与缺陷分级 | 2 | 🔴 P0 | 减少人类决策模糊性 |
| C. 部分交付与Pipeline回滚 | 2 | 🟡 P1 | 提升交付弹性与容灾 |
| D. 测试体系增强（回归/增量） | 2 | 🟡 P1 | 中长期质量保障 |
| E. 上下文与资源管理 | 4 | 🟡 P1 | 防止长流程Agent质量退化 |
| F. 数据竞争与产物校验 | 4 | 🟢 P2 | 防御性机制，可渐进引入 |
| G. 前置需求探索（Stage0） | 3 | 🟢 P2 | 改善PRD输入质量 |
| H. 实现层操作细节 | ~6 | 🟢 P3 | 适合沉淀到SKILL.md而非白皮书 |

---

## 三、融合建议正文

### A 类：模块合并与测试执行精细化 🔴 P0

#### A-1 Stage4.1 后端模块合并子流程精确化

**来源**：flash 建议2.3
**现状**：v2.2 §4.1仅有"合并各模块路由到统一入口，验证全局Schema一致性，运行所有模块单元测试"三句话。
**问题**：多Agent并行开发后的合并涉及路由冲突、中间件链顺序、Schema引用解析等，过于简化。

**建议**：在 §4.1 补充合并子流程：

> **后端模块合并四步流程**：
>
> Step 1 合并前检查：
> - 确认所有模块状态为 DONE（非 BLOCKED/DEFER）
> - 验证依赖链闭环——各模块的依赖声明与最终状态一致
> - Schema冲突检测——检查不同模块是否定义了同名但不同类型的字段
> - 路由冲突检测——无重复的 HTTP Method + Path 组合
>
> Step 2 路由聚合：
> - 收集所有模块 routes.ts 中的路由定义
> - 按路径前缀分组，挂载到统一 Hono app
> - 验证中间件链顺序（auth → body-parser → router → error-handler）
>
> Step 3 Schema引用解析：
> - 检查每个模块 schema.ts 对全局 schema 的 import 路径正确性
> - 运行 Drizzle Schema 编译验证
>
> Step 4 合并后验证：
> - 启动应用（至少 import 级验证，确认无启动异常）
> - 运行全量单元测试（必须全部通过）
> - 运行全量集成测试

#### A-2 增量测试执行窗口（嵌入Stage3）

**来源**：flash 建议2.4
**现状**：Stage2写大量测试用例→Stage4才执行，中间存在"验证真空"。TDD循环中的单元测试是模块级的，模块间集成测试要等Stage4。
**问题**：一个模块开发完成到Stage4发现集成问题，反馈周期过长。

**建议**：在 §3.3 末尾或 §3.1 末尾补充：

> **增量集成测试**（嵌入Stage3，非独立Stage）：
> - 每轮调度中，模块标记DONE后，立即执行该模块对应的 integration-tests/modules/ 测试
> - 该模块依赖的模块已DONE时，执行涉及的跨模块 scenarios/ 测试子集
> - 输出增量测试报告，发现失败立即反馈给对应Agent
> - Stage3全部模块DONE后，执行全量集成测试作为进入Stage4的门禁
>
> 增量测试 ≈ 每模块DONE后10秒级快速反馈；全量测试 ≈ Stage3末分钟级门禁关卡。

#### A-3 ↺循环问题分级处理矩阵

**来源**：flash 建议2.5
**现状**：v2.2 §2.3有"3轮上限+强制降级"，但不同问题类型混在一起处理。
**问题**：字段命名风格问题和实体关系冲突问题不应走相同的3轮循环路径。

**建议**：在 §2.3 强制降级策略之前增加问题分级：

> **↺循环问题分级**（按严重度分流，避免低价值问题浪费循环轮次）：
>
> | 级别 | 问题类型示例 | 处理方式 | 轮次上限 |
> |------|-------------|---------|---------|
> | 自愈级 | 字段命名风格、注释语言、缩进格式 | ①或②自行修正，不进grill循环 | 0 |
> | 调试级 | 字段类型选择、模块边界微调 | ①/②修正后grill单轮验证 | 2轮 |
> | 协商级 | 验收标准覆盖范围、API响应字段增减 | ①与②协商后grill验证 | 3轮 |
> | 上浮级 | 实体关系冲突（1:N vs N:M）、核心流程分歧 | 立即输出未决清单，上浮人类 | 首轮即上浮 |
> | 终止级 | PRD本身存在根本矛盾、技术方案不可行 | 立即终止循环，上浮人类重评 | 首轮即终止 |

---

### B 类：部分交付与Pipeline回滚 🟡 P1

#### B-1 部分交付机制（DEFER 状态）

**来源**：flash 建议3.3，bypro部分涉及
**现状**：v2.2仅有BLOCKED状态，无"主动推迟"机制。一个模块复杂度过高会阻塞全Pipeline。
**问题**：MVP场景常见"某模块暂时做不了但其他模块可以交付"。

**建议**：在 §9.3 文件状态约定中增加 DEFER 状态，或在 §3.0 防错机制表中补充：

> **DEFER 状态**（主动推迟，不同于被动BLOCKED）：
>
> 触发条件：
> - 模块经2轮重新分配仍未完成（复杂度超出预期）
> - 或依赖的外部服务/接口无法就绪
> - 或需求存在争议需长周期讨论
>
> 处理流程：
> 1. Coordinator标记该模块为DEFER（`DEFER`标记文件 + `reason.md`）
> 2. 依赖该模块的下游模块级联标记为DEFER
> 3. 其余模块继续执行，不受影响
> 4. DEFER模块进入下一迭代的Backlog，本轮交付标注"部分交付"
>
> 文件状态新增：`DEFER` + `reason.md`

#### B-2 Pipeline级回滚协议

**来源**：flash 建议3.4
**现状**：v2.2有§0.3回退协议（Stage3→Stage2的回退），但缺少Stage4集成阶段发现大面积问题的回滚机制。
**问题**：Stage4集成测试发现>3个P0缺陷时，没有一个系统性的回滚路径。

**建议**：在 §4.3 或 §4.4 之后补充：

> **Stage4回滚协议**（触发条件：任一满足且人类确认后执行）：
> 1. 集成测试发现P0缺陷 > 3个
> 2. Bug修复引入了新的P0缺陷
>
> 回滚路径：
> - 回滚目标：Stage3最后一个全量测试通过的检查点
> - 回滚范围：保留所有模块代码，状态回退到检查点
> - 操作：读取`pipeline-state.json`上一个绿色检查点→对比差异→移除检查点后变更（保留Bug修复）→重置模块状态→重新执行Stage4门禁
>
> 风险控制：
> - 回滚是手动触发（需人类确认）
> - 回滚前Git备份当前代码
> - 回滚后仍无法通过→标记"本轮不可交付"，进入Stage5复盘

---

### C 类：测试体系增强 🟡 P1

#### C-1 回归测试自动增长机制

**来源**：flash 建议3.5
**现状**：v2.2 §4.4提到"每次修复补充回归测试"，但未定义回归测试的目录结构和管理方式。
**问题**：回归测试散落在modules/或scenarios/中，难以追踪和管理。

**建议**：在 §4.4 Bug修复循环中补充：

> **回归测试目录与管理**：
>
> 目录结构新增 `integration-tests/regression/`：
> - 每个Bug修复必须创建一个回归测试文件：`regression/bug-<编号>-<简述>.test.ts`
> - 回归测试必须独立（不依赖其他测试状态），包含：Bug复现步骤+断言+修复验证
> - 每次全量集成测试时regression/必须全部执行
> - 回归测试套件单调增长（只增不减），确保历史Bug不重现
> - 超过5轮迭代无失败的回归测试可标记为"低价值"但不删除

#### C-2 前端Mock对接流程细化

**来源**：flash ACC-06
**现状**：v2.2已有Mock持续验证，但前端如何连接Mock、Mock地址如何注入的细节未覆盖。
**问题**：不同Agent可能对Mock连接方式有不同理解，导致集成时混乱。

**建议**：在 §3.2 前端开发章节补充：

> **前端Mock连接规范**：
> - Mock地址统一通过环境变量 `VITE_API_BASE_URL` 注入
> - 开发模式下 `.env.development` 指向Mock服务（如 `http://localhost:3001`）
> - Stage4联调时切换 `.env.production` 指向真实API
> - 页面代码中不硬编码API地址，统一从 `api.config.ts` 读取
> - Mock数据更新后，前端通过 `npm run mock:sync` 刷新本地Mock类型定义

---

### D 类：上下文与资源管理 🟡 P1

#### D-1 上下文窗口分阶段管理

**来源**：flash 建议3.2
**现状**：v2.2 §6.4有上下文摘要格式（70%触发刷新），但缺少Stage边界的自动重置策略。
**问题**：同一Agent跨Stage时上下文持续膨胀。

**建议**：在 §6.4 上下文耗尽处理中补充：

> **Stage边界上下文重置**：
> - 每个Stage结束→当前Agent输出写入文件系统→下一Stage Agent为新spawn实例（上下文归零）
> - 例外：Coordinator跨Stage复用时，每轮调度前执行上下文精简（保留当前状态+分配日志，丢弃已完成模块的调试信息）
> - Long-running Agent上下文超80%窗口→记录WARNING→下一可用边界强制重置

#### D-2 Agent执行时限契约

**来源**：flash 建议3.1
**现状**：v2.2 §11.2有心跳机制（60s超时检测），但无不区分Agent类型的差异化时限。
**问题**：PM Agent写PRD 2小时正常，Backend Agent单模块超2小时则异常——统一心跳时长不够精确。

**建议**：在 §11.2 心跳机制后补充各Agent差异化时限：

> **Agent执行时限参考**（超时分级：WARNING=80% / ESCALATE=120% / TERMINATE=200%）：
>
> | Agent角色 | 建议时限 | 说明 |
> |----------|---------|------|
> | PM Agent | 120min | PRD复杂度差异大 |
> | 架构专家 | 60min | Schema+契约一次性输出 |
> | 业务领域专家 | 60min | 按PRD复杂度分级 |
> | Mock专家 | 60min | 全量Mock一次性搭建 |
> | 单模块测试 | 120min | 按模块数分摊 |
> | Backend Agent | 30min/模块 | 超2倍→Coordinator标记STUCK |
> | Frontend Agent | 30min/页面 | 同上 |
> | Code Review | 15min | 超过→超时降级为快速审查 |
> | Coordinator | 持续运行 | 不设时限，靠检查点恢复 |

#### D-3 模块定义摘要传输（减负）

**来源**：bypro 建议4.3 模块定义摘要
**现状**：v2.2未定义Coordinator传给Agent的模块信息格式。
**问题**：完整`<module>.md`可能包含大量Agent不需要的信息。

**建议**：在 §3.0 Coordinator职责中补充：

> **模块分配信息摘要**（Coordinator传给Agent时不传完整`<module>.md`，传摘要版YAML）：
> ```yaml
> module: product
> domain: 业务核心
> depends_on: [user]
> apis:
>   - GET /api/products (list)
>   - POST /api/products (create)
>   - GET /api/products/:id (detail)
> tables:
>   - categories (id, name, parent_id)
>   - products (id, name, category_id, price)
> acceptance:
>   happy_path: 3
>   exception_path: 4
> ```
> Agent需要完整信息时通过文件路径按需读取。

#### D-4 产出物版本元数据

**来源**：bypro 建议4.2
**现状**：v2.2有§0.6版本标记机制（文件名+锁定版），但缺少文件内部元数据版本号。
**问题**：↺循环多轮修改时，仅靠文件名后缀区分数个中间版本不够。

**建议**：在 §0.6 版本标记机制后补充：

> **产出物内部版本元数据**（轻量，不引入额外工具）：
> 文件头部增加注释块：
> ```yaml
> # @version: 1.2
> # @last_modified: 2026-05-21T14:30:00Z
> # @modified_by: architect-agent
> # @change: 拆分trace_code字段
> # @grill_round: 2
> ```
> ↺循环中每次修正后自动更新版本号和变更说明。旧版本通过Git历史自然保留。锁定版在Git中打tag：`stage2-locked/v1.0`。

---

### E 类：数据竞争与产物校验 🟢 P2

#### E-1 模块间数据竞争自动检测

**来源**：bypro 建议4.6
**现状**：v2.2有依赖图自动校验，但未检测操作同一张表的并发风险。
**问题**：两个模块操作同一表的不同字段时，可能产生数据竞争（如模块A扣库存+模块B改价格同时发生）。

**建议**：在 §3.0 启动时依赖图自动校验后补充：

> **数据竞争扫描**（每个领域所有模块DONE后执行一次）：
> - 提取该领域所有模块的schema定义
> - 检测是否存在两个模块操作同一张表（即使不同字段）
> - 检测外键关联表上的操作时序是否正确
> - 发现潜在竞争→写入`race-condition-warnings.md`→Stage4联调重点验证

#### E-2 产出物完整性自动校验

**来源**：bypro 建议4.7
**现状**：v2.2各阶段有门禁，但无自动校验产出文件完整性（大小、格式、哈希）的机制。
**问题**：文件损坏/格式错误/空文件可能导致下游Agent读取失败且无告警。

**建议**：在 §2.3（↺门禁）、§3.0（Stage3准入）等位置增加校验：

> **阶段门禁产出物完整性校验**：
>
> | 阶段门禁 | 校验项 | 方式 |
> |---------|--------|------|
> | Stage1→Stage2 | PRD文件>1KB；9个章节标题全存在 | 文件系统+正则 |
> | Stage2→Stage3 | 所有产出物存在且MD5与↺锁定时刻一致；YAML/JSON语义可解析 | 文件hash+格式验证 |
> | Stage3→Stage4 | 所有模块目录存在DONE标记；schema.sql语法正确 | 标记文件检查+SQLite dry-run |
> | Stage4→交付 | 集成测试报告非空；0个P0/P1 Bug | 文件内容解析 |
>
> 校验失败→门禁拒绝→输出具体缺失/损坏项清单。

#### E-3 产物依赖追踪图

**来源**：kimi R3 机制2
**现状**：各产出物间隐式依赖未被显式追踪。
**问题**：Coordinator无法自动判断下游Agent是否缺少必要的前置产出物。

**建议**：在 §9.5 知识共享协议中补充：

> **产物依赖声明**：每个产出物文件头增加`依赖:`字段：
> ```
> 依赖: PRD.md, spec.locked.md, schema.locked.sql
> ```
> Coordinator启动时解析全部依赖图，发现缺失依赖→阻止进入下一阶段。

#### E-4 Mock-实现变更同步协议

**来源**：bypro 建议4.5
**现状**：v2.2有Mock持续验证，但契约锁定后如发现不合理需要变更时的同步流程不完整。
**问题**：开发中改契约→Mock未同步→前端基于旧Mock开发→联调发现不匹配。

**建议**：在 §2.4 Mock生命周期后补充：

> **Mock-实现变更同步**（契约锁定后仍需变更时）：
> ```
> 开发Agent发现契约不合理
>   → 提交变更申请(change-request.md)
>   → Coordinator暂缓该模块(BLOCKED=contract_change_pending)
>   → 人类审查（小变更<3接口可自动批准）
>   → 批准后：更新api-contract.yaml → Mock Agent同步更新 → 受影响前端Agent收到通知 → 前端对应页面标记"需重新联调"
> ```
> Coordinator自动分析：读取变更接口→扫描所有前端页面引用的API→输出受影响页面清单。

---

### F 类：前置需求探索（Stage0）🟢 P2

#### F-1 需求预处理步骤（Stage0）

**来源**：flash 建议1.1，bypro T1部分涉及
**现状**：v2.2 Stage1从"原始需求"直接跳到PRD创作。
**问题**：缺少用户角色定义、价值流映射等预处理，低质量输入直接进入PRD。

**建议**：在 §1 Stage1开头之前增加轻量预处理步骤：

> **Stage1前置：需求精炼（轻量，10-15分钟）**
>
> PM Agent收到原始需求后，在PRD创作前完成：
> 1. **用户角色快速定义**：列出2-4个核心用户角色，每角色一句话描述+核心痛点
> 2. **价值流一句话**：核心业务流程从触发到交付的一句话链路
> 3. **需求精炼**：识别模糊陈述→标记需澄清项；识别矛盾→标记需决策项
>
> 产出：以上内容嵌入PRD「项目背景」章节，不产生独立文件。

#### F-2 PRD"不做"清单显式化

**来源**：flash 建议1.2
**现状**：v2.2 PRD质量门禁有"范围边界清晰"，但PRD模板未要求显式列出"不做"清单。
**问题**：没有显式"不做"清单，Stage2/3容易出现范围蔓延。

**建议**：在 §1 PRD必须包含的表格中增加一行：

> | 明确的"不做"清单 | 本版MVP明确不实现的功能、场景、边界 | 防止Stage2/3范围蔓延，限定Scope |

---

### G 类：实现层操作细节 🟢 P3

> 以下建议属于Skill/Agent实现层细节，建议纳入对应 SKILL.md 而非白皮书正文。

#### G-1 调度器文件扫描伪代码（byminimax 1.1）

纳入 `kf-pipeline-coordinator/SKILL.md`，补充Coordinator扫描模块状态的TypeScript伪代码示例。

#### G-2 Stage2门禁检查代码实现（byminimax 3.2）

纳入 `kf-pipeline-coordinator/SKILL.md` 或 `kf-mvp-health-check/SKILL.md`，补充Stage2门禁的自动化检查实现。

#### G-3 Mock-后端契约一致性验证脚本（byminimax 3.3）

纳入 `kf-mvp-mock-service/SKILL.md`，补充Mock与真实API的契约对比脚本示例。

#### G-4 Agent配置文件修改对照表（flash 建议1.5/2.6/3.7）

纳入各对应 SKILL.md，具体Agent的system prompt调整指南按Skill粒度分发。

#### G-5 文件竞态防护详细写入规范（flash 建议3.6）

纳入 `kf-pipeline-coordinator/SKILL.md`，补充.tmp→原子重命名的具体实现规范。

#### G-6 Quality Gate检查清单（flash 建议1.4）

纳入 `kf-mvp-health-check/SKILL.md`，作为可执行的Checkbox清单。

---

## 四、优先级速查

| 优先级 | 编号 | 建议 | 影响 | 工作量 |
|--------|------|------|------|--------|
| 🔴 P0 | A-1 | Stage4.1模块合并子流程精确化 | 消除Stage4实操模糊 | 低 |
| 🔴 P0 | A-2 | 增量测试执行窗口 | 缩短反馈周期 | 中 |
| 🔴 P0 | A-3 | ↺循环问题分级处理矩阵 | 减少无价值循环 | 低 |
| 🟡 P1 | B-1 | 部分交付DEFER机制 | 解除全流程阻塞 | 低 |
| 🟡 P1 | B-2 | Pipeline级回滚协议 | 补全容灾空白 | 低 |
| 🟡 P1 | C-1 | 回归测试增长机制 | 中长期质量保障 | 低 |
| 🟡 P1 | C-2 | 前端Mock对接流程细化 | 消除集成混乱 | 低 |
| 🟡 P1 | D-1 | 上下文分阶段管理 | 防止Agent质量退化 | 低 |
| 🟡 P1 | D-2 | Agent执行时限契约 | 精准异常检测 | 低 |
| 🟡 P1 | D-3 | 模块定义摘要传输 | 降低上下文消耗 | 低 |
| 🟡 P1 | D-4 | 产出物版本元数据 | 可追溯性 | 低 |
| 🟢 P2 | E-1 | 数据竞争自动检测 | 防御性机制 | 中 |
| 🟢 P2 | E-2 | 产出物完整性校验 | 防御性机制 | 中 |
| 🟢 P2 | E-3 | 产物依赖追踪图 | 自动化校验 | 中 |
| 🟢 P2 | E-4 | Mock-实现变更同步协议 | 减少漂移 | 中 |
| 🟢 P2 | F-1 | Stage0需求预处理 | 改善PRD输入质量 | 低 |
| 🟢 P2 | F-2 | PRD"不做"清单显式化 | 防范围蔓延 | 低 |
| 🟢 P3 | G-1~6 | 实现层操作细节（6项） | Skill层补充 | 低 |

---

## 五、实施路线图建议

```
第一阶段（本周）：P0 三项
  ├── A-3 ↺循环问题分级矩阵 → 修改白皮书 §2.3
  ├── A-1 Stage4.1合并子流程 → 修改白皮书 §4.1
  └── A-2 增量测试执行窗口 → 修改白皮书 §3.3末尾

第二阶段（下迭代）：P1 八项
  ├── B-1 DEFER机制 → 修改白皮书 §9.3/§3.0
  ├── B-2 Pipeline回滚 → 修改白皮书 §4.3
  ├── C-1 回归测试增长 → 修改白皮书 §4.4
  ├── C-2 Mock对接规范 → 修改白皮书 §3.2
  └── D-1~4 上下文/时限/摘要/版本 → 修改白皮书各对应位置

第三阶段（按需）：P2 六项 + P3 六项
  ├── E-1~4 数据竞争/校验/依赖/同步 → 渐进引入
  ├── F-1~2 Stage0预处理 → 可选引入
  └── G-1~6 → 分发到各 SKILL.md
```

---

*融合版生成时间：2026-05-21*
*融合来源：bykimi.md + byminimax.md + bypro.md + flash.md，对比基准 v2.2*
*未吸收建议共 18 项白皮书级 + 6 项Skill级，建议分三阶段渐进实施*
