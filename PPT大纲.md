# PPT 大纲：AI 写代码，为什么需要一条"流水线"

> 项目：all-in-mvp（QoderMVP 多 Agent 并行开发流水线）
> 结构：为什么 → 做什么 → 好在哪 → 凭什么

---

## P1 封面

- 标题：**all-in-mvp —— 把 AI 开发从"手工作坊"变成"流水线工厂"**
- 副标题：一行命令安装，从需求到交付全自动，还省 60-90% 的 Token

## P2 一句话总结（先给结论）

- AI 很聪明，但它**不稳定、爱发挥、烧钱快**
- 我们做的事：给 AI **上规矩、上流水线、上电表**
- 结果：同样的需求，谁来跑、跑几次，出来的东西都一样，而且更便宜

---

# 第一部分：为什么（痛点）

## P3 痛点一：AI 不幂等——同一个模型，两次答案不一样

- 大白话：你让 AI 写同一个页面，今天写的和明天写的长得不一样
- 根因：模型输出本身有随机性；更要命的是**上下文不同**——聊天记录、打开的文件、之前的对话都会影响它的"发挥"
- 后果：项目里页面风格五花八门、代码写法各写各的，改一处要看懂三种风格

## P4 痛点二：AI 从零写前端 = 最贵最不稳的活

- 每次从零搭项目：选框架、配构建、写组件……几万 Token 就烧掉了
- 而且这部分恰恰是 AI **最容易翻车**的地方（依赖版本冲突、配置写错、样式跑偏）
- 大白话：让 AI 每次都"从和面开始做包子"，又慢又贵还经常做塌

## P5 痛点三：Token 就是钱，但没人在记账

- 多 Agent 并行开发时，主会话 + 子 Agent + Agent 间通信，Token 消耗是黑盒
- 大白话：家里十个电器同时开，却没装电表——月底账单吓一跳，还不知道谁费电

## P6 痛点四：AI 开发缺工程化——"能跑"不等于"能交付"

- AI 写完说"好了"，但没测试、没验收标准、没回归——改一处坏三处
- 传统软件工程有的东西（契约、测试、审查、门禁），AI 开发流程里基本是空白

---

# 第二部分：做什么（方案）

## P7 总体方案：一条流水线 + 三根支柱

- **流水线**：43 个技能组成的多 Agent 并行开发管线（需求 → 架构 → 契约 → TDD 开发 → 集成 → 验收）
- 三根支柱：
  1. **脚手架固化**（治不稳定）
  2. **极致节能引擎**（治烧钱）
  3. **E2E 质量门禁**（治不可交付）

## P8 支柱一：前端脚手架固化（liquid-glass-frontend）

- 技术栈**焊死**：Vue 3 + Vite + shadcn + Liquid Glass 设计体系，AI 无权换
- AI 不再"从零发挥"，而是在**统一底座上填业务**
- 大白话：不让 AI 现场和面，直接给它标准面团——它只负责包馅
- 效果：项目结构零偏差、界面风格超高一致、砍掉最贵最不稳的初始化环节

## P9 支柱二：多 Agent 流水线（分工 + 契约）

- 像工厂分工：产品经理 Agent 写 PRD → 架构 Agent 定契约 → 后端 TDD Agent、前端 Agent **并行**开发 → 审查 Agent 交叉拷问（grill）
- 关键机制：**先锁契约再动工**——API 契约、验收标准先定死，各 Agent 照图施工，互不猜测
- 大白话：不是一个全能工人从头干到尾，而是各岗位拿着同一张图纸同时开工

## P10 支柱三：极致节能引擎（ultra-cost-effective）

- 项目级全链路 Token 监控：主会话、子 Agent、Agent 间通信全部穿透记账
- 七层节能架构：上下文压缩、KV Cache 共享前缀、模型智能路由（简单活用便宜模型）、输出压缩
- 大白话：装电表 + 换节能灯 + 峰谷用电，三管齐下
- 综合节省 **60-90%** Token，且不降低输出质量

## P10-A 节能引擎：工作流程（三步走）

- **第 1 步 安装接入**：把引擎目录拷进项目 → 合并 settings.json（注册 Hooks）→ 部署平台 Hook（Qoder 用 `.cmd` 脚本放进 `~/.qoder/hooks/`）
- **第 2 步 运行时自动生效**（装完就不用管）：
  - 每次跑构建/测试命令 → PreToolUse Hook 自动追加输出压缩（tokenforge）
  - 每次工具调用后 → PostToolUse Hook 记账 + 三色灯监控上下文健康（🟢<60% / 🟡60-80% / 🔴>80%，超标自动注入压缩提示）
  - 每次派生子 Agent → agent-spawn-guard 前置拦截，注入压缩过的会话记忆，砍掉重复上下文
- **第 3 步 事后记账**：token-watcher / project-monitor 扫描平台落盘的会话记录（JSONL）→ 写入 tracker 文件 → `token report` 出报表
- 三档预设随时切换：quick（省 50-70%）/ standard（70-85%，推荐）/ extreme（85-95%）

## P10-B 节能引擎：原理（为什么敢说不降质量）

- **核心设计：零侵入**——不拦截 API 请求、不改 BASE_URL、不碰模型推理过程，只做三件事：
  1. **压缩喂给模型的东西**（工具输出、上下文、Agent 间通信）——模型收到的是去冗余版，但原文都留着，随时可通过 session-memory 索引取回
  2. **让缓存多命中**（KV Cache 共享前缀）——重复内容按缓存价计费，DeepSeek 缓存命中价约为原价的 1/120
  3. **建议用对的模型**（L7 路由）——分析你的提问："架构/设计/规划" → 推荐 Pro，"写代码/修 bug/测试" → 推荐 Flash；上下文过长自动倾向降级 Flash
- 大白话：不给模型"降智"，只是不让它反复读废话、不让杀鸡用牛刀
- 监控同理零侵入：只读平台自己写在磁盘上的会话记录，算账不干扰干活

## P10-C 节能引擎：能干什么 / 不能干什么（说清边界）

**✅ 适用范围**

- 平台：Claude Code 和 Qoder 双平台（本项目已收敛为 Qoder 专属适配）
- Claude Code 上：JSONL 自带 usage 字段 → Token 统计**精确到每次调用**，可与 DeepSeek 账单对账（误差 <5%）
- 不依赖特定模型的部分（输出压缩、session-memory）：任何模型都能用
- KV Cache / 模型路由：需要 DeepSeek API（Pro / Flash 双模型）

**❌ 不支持范围（已核实，如实说）**

- **不能精确监听 Qoder auto 模式**：Qoder 落盘的会话记录里没有 usage、model、timestamp 字段——既不知道 auto 实际选了哪个模型，Token 量也只能按文本长度**估算**（有误差），做不到 Claude Code 那种精确对账
- **不能给不同子 Agent 强制绑定不同 BYOK 模型**：引擎的模型路由只覆盖 DeepSeek Pro/Flash 两个模型，且是"**建议式**"（把推荐注入上下文，不强制切换）；子 Agent 实际用什么模型由 Qoder 平台调度决定，技能里的 recommended_model 只是声明期望，平台不保证执行
- **不能实时熔断超支**：因为不拦截网络流量，只能事后记账 + 上下文超标时注入提示，做不到"到额度直接掐断请求"
- **换其他模型要手工扩展**：路由规则和定价写在 presets/ 配置里，接入 DeepSeek 以外的模型需要自己改配置
- 大白话：它是"电表 + 节能开关"，不是"总闸"——能记账、能省，但不能替平台做主

## P11 支柱四：工程化质量体系

- PRD 驱动测试：每条需求可追溯到 E2E 测试场景（PRD → 测试矩阵）
- 一键全量回归：环境消毒 → 数据重置 → 冒烟 → 全量 E2E → 覆盖率门禁
- 大白话：AI 说"做完了"不算数，机器验收通过才算数

---

# 第三部分：好在哪（收益）

## P12 收益对比表（建议做成左右对比图）

| 维度 | 裸用 AI | 用 all-in-mvp |
|------|---------|---------------|
| 稳定性 | 每次输出都不一样 | 脚手架 + 契约固化，结构零偏差 |
| 界面 | 风格随机漂移 | 统一设计体系，交互高度一致 |
| 成本 | Token 黑盒消耗 | 全链路记账，省 60-90% |
| 速度 | 串行、反复返工 | 多 Agent 并行开工 |
| 质量 | "看起来能跑" | 测试门禁，验收可追溯 |
| 上手 | 需要 prompt 高手 | 一行命令安装，开箱即用 |

## P13 核心洞察（这页升华）

- AI 时代的竞争力不在"模型多聪明"，而在**"怎么把不确定的 AI 关进确定的工程流程里"**
- 我们把随机性关进三个笼子：**脚手架锁住技术栈、契约锁住接口、测试锁住行为**
- 模型可以换、会话可以断，但产出物的质量下限被流程托住了

---

# 第四部分：凭什么（证据与机制）

## P14 凭什么之一：机制上就杜绝了发挥空间

- 不靠"提醒 AI 别乱来"（提醒会被遗忘），靠**结构性约束**：
  - 脚手架是现成文件，AI 只能改业务代码
  - 契约先锁定（grill 交叉拷问审查通过才放行）
  - 增量变更有分级决策树和回退协议（白皮书 v2.6）

## P15 凭什么之二：省钱是可测量的，不是口号

- project-monitor 全链路监控：每次会话、每个子 Agent 的 Token 都有账
- 理论节约按七层逐层归因，Flash/Pro 成本实时对比
- 大白话：省了多少，报表说话

## P16 凭什么之三：质量有门禁，不靠自觉

- E2E 覆盖率有最低标准，不达标不放行
- 有头/无头测试一致性校验，防"演示能过、真跑翻车"
- 每次交付后有复盘（retrospective），流程本身也在迭代

## P17 凭什么之四：真的能一行装上

- `npx giget gh:1234567KF/all-in-mvp#all-in-mvp .` —— 43 个技能 + 节能引擎 + 脚手架全到位
- 项目级部署、不污染全局；tag 锁版本，可复现

---

## P18 结尾页

- 一句话收尾：**"我们不是让 AI 更聪明，而是让 AI 的聪明变得可复制、可验收、可负担。"**
- 下一步 / QA
