# 白皮书自我优化严谨性审视与3轮迭代建议

> **审视日期**：2026-05-21
> **审视视角**：白皮书自身的反思能力 — 它是否具备让方法论自我进化的严谨机制？
> **核心问题**：白皮书能发现执行中的问题（↺拷问），但它能发现**自身方法论的问题**吗？

---

## 一、白皮书自我优化能力诊断

### 1.1 已有的自我修正机制（操作层）

白皮书已经具备相当的操作层自我修正能力：

| # | 机制 | 位置 | 修正什么 | 严谨度 |
|---|------|------|---------|--------|
| G1 | ↺ 拷问审查循环 | Stage2.3 | ①架构与②业务的产出物不一致 | ⭐⭐⭐ 四维审查+3轮上限 |
| G2 | Code Review | Stage3.1 | 代码实现与契约的偏差 | ⭐⭐ 有触发条件但无通过标准量化 |
| G3 | 阶段门禁 | 全局 | 防止跨阶段跳跃 | ⭐⭐⭐ 硬约束 |
| G4 | 依赖图防错 | Stage3.0 | 遗漏/重复/死锁 | ⭐⭐⭐ 四项全防 |
| G5 | 变更评审 | Stage1/2 | 锁定后随意修改 | ⭐⭐ 有流程但无评审人 |
| G6 | 3轮降级策略 | Stage2.3 | ↺循环僵持 | ⭐⭐ 有人类兜底 |

**但所有这些机制的共同特征：“它们能发现错误，但不能发现为什么总是犯同类错误。”**

### 1.2 缺失的方法论层自我优化（元层面）

| # | 缺失项 | 本质问题 | 严重级别 | 一句话诊断 |
|---|--------|---------|---------|-----------|
| R1 | **无量化质量指标体系** | 所有判断是二元（过/不过），无灰度 | 🔴 CRITICAL | 不知道"差"有多差，"好"有多好 |
| R2 | **无↺拷问有效性标定** | grill过了≠grill准了；可能过松或过严 | 🔴 CRITICAL | 拷问者自己不被拷问 |
| R3 | **无项目后回溯闭环** | Stage4交付后，经验不反哺方法论 | 🔴 CRITICAL | 每次从零开始，不积累 |
| R4 | **无错误分类学** | Bug原因没有分类体系，无法识别模式 | 🟡 HIGH | 修了Bug但不修Bug的根 |
| R5 | **无统计过程控制** | 没有缺陷率、返工率、首次通过率 | 🟡 HIGH | 不知道流程是否在改善还是恶化 |
| R6 | **无方法论版本演进** | 白皮书自身没有版本号和修订历史 | 🟡 HIGH | 改了白皮书不知道改了什么 |
| R7 | **无对比基线** | 没有与非并行开发的对比数据 | 🟠 MEDIUM | 声称更快但没有证据 |
| R8 | **无门禁有效性审计** | 门禁通过了，但门禁本身有效吗？ | 🟡 HIGH | 门禁可能是"稻草人" |
| R9 | **无Agent能力标定** | 不同Agent的质量参差不齐但无法量化 | 🟠 MEDIUM | 不知哪个Agent在拖后腿 |
| R10 | **无渐进式收紧机制** | 质量门禁一成不变，不会随项目成熟而提高 | 🟡 HIGH | 没有"越做越好"的内置趋势 |

### 1.3 关键洞察：白皮书有"免疫系统"但无"学习系统"

```
操作层修正（已有）：发现bug → 修正bug → 回归验证
    ✅ 白皮书已具备

方法论层进化（缺失）：分析bug模式 → 修正流程弱点 → 更新方法论 → 验证改进效果
    ❌ 白皮书完全不具备
```

**形象比喻**：白皮书像一个有免疫系统的生物——能对抗感染，但不能进化出更强的免疫系统。每次项目独立作战，经验无法遗传。

---

## 二、Round 1：建立量化与测量体系（痛点R1-R5，植入"数字神经系统"）

### 2.1 建立质量指标仪表盘

> **位置**：在Stage4末尾新增「第十一章：质量度量与回溯」

```markdown
## 十一、质量度量与持续改进

### 11.1 项目级关键质量指标（KQI）

每个项目运行结束后，Coordinator自动采集以下指标：

#### 契约阶段（Stage2）指标

| 指标 | 计算方式 | 目标值 | 告警值 |
|------|---------|--------|--------|
| **Grill首次通过率** | ↺第1轮通过的审查维度数 / 4（四维） | ≥ 75%（3/4） | < 50%（2/4） |
| **Grill收敛速度** | 通过所有维度所需轮次 | ≤ 2轮 | ≥ 3轮 |
| **Grill问题密度** | 发现的Issue总数 / 模块数 | ≤ 2个/模块 | ≥ 5个/模块 |
| **术语不一致数** | PRD术语定义中不一致的术语数量 | 0 | ≥ 3 |
| **验收标准覆盖率** | 有模块覆盖的PRD验收标准数 / PRD验收标准总数 | 100% | < 90% |

#### 执行阶段（Stage3）指标

| 指标 | 计算方式 | 目标值 | 告警值 |
|------|---------|--------|--------|
| **首次Code Review通过率** | 一次Review通过的模块数 / 总模块数 | ≥ 70% | < 40% |
| **P0问题密度** | P0问题总数 / 模块数 | ≤ 1个/模块 | ≥ 3个/模块 |
| **TDD循环效率** | 每个模块的Red-Green-Refactor总轮次 | ≤ 5轮/模块 | ≥ 10轮/模块 |
| **Agent重试率** | 需要重试的Agent数 / 总Agent数 | ≤ 10% | ≥ 30% |
| **并行度利用率** | 实际并行Agent数 / 最大并行Agent数 | ≥ 80% | < 50% |

#### 集成阶段（Stage4）指标

| 指标 | 计算方式 | 目标值 | 告警值 |
|------|---------|--------|--------|
| **Mock-后端一致率** | 接口一致的Mock端点数 / 总Mock端点数 | 100% | < 95% |
| **联调问题密度** | 联调发现问题数 / 模块数 | ≤ 1个/模块 | ≥ 3个/模块 |
| **Bug逃逸率** | Stage4发现的Bug数 / (Stage3+Stage4总Bug数) | ≤ 20% | ≥ 50% |
| **回归测试通过率** | 修复后回归测试首次通过数 / 修复总数 | ≥ 90% | < 70% |

### 11.2 指标采集自动化

所有指标由Coordinator自动采集，写入 `pipeline-metrics.json`：

```json
{
  "project_id": "iteration-15",
  "stage2": {
    "grill_first_pass_rate": 0.75,
    "grill_convergence_rounds": 2,
    "grill_issue_density": 1.8,
    "terminology_inconsistencies": 0,
    "acceptance_coverage": 1.0
  },
  "stage3": {
    "first_cr_pass_rate": 0.6,
    "p0_density": 0.8,
    "tdd_avg_rounds": 4.2,
    "agent_retry_rate": 0.08,
    "parallelism_utilization": 0.85
  },
  "stage4": {
    "mock_api_consistency": 0.98,
    "integration_issue_density": 0.5,
    "bug_escape_rate": 0.15,
    "regression_pass_rate": 0.92
  }
}
```

> **原则**：不以指标考核Agent（避免Goodhart效应），指标只用于发现流程瓶颈。
```

### 2.2 Grill审查有效性标定

```markdown
### 11.3 ↺拷问审查质量标定

**问题**：grill通过了，但grill本身可能遗漏（假阴性）或误报（假阳性）。如何标定grill的有效性？

**方法：延迟验证——用Stage4的"事后真相"标定Stage2的"事前审查"**

在Stage4完成后，回填grill质量报告：

| 标定指标 | 计算方式 | 数据来源 | 标定目的 |
|---------|---------|---------|---------|
| **Grill假阴性率** | Stage4发现但grill未发现的需求遗漏数 / Stage4总需求遗漏数 | 联调问题+集成测试Bug | grill是否过松？ |
| **Grill精准度** | grill标记的Issue中后来被确认有效的比例 | grill报告 vs Stage4验证 | grill是否过严？ |
| **Grill覆盖盲区** | Stage4发现的问题中哪种类型grill从未发现 | 按问题类型分类统计 | grill的审查维度是否需要扩充？ |

**标定周期**：每3个项目运行一次grill标定，决定是否需要调整grill审查维度、增加审查项或调整审查严格度。

**自适应调节**：
- 连续3个项目假阴性率 > 20% → 触发grill审查维度扩充（如增加"数据一致性"维度）
- 连续3个项目Grill精准度 < 50% → 触发grill审查标准调优（降低误报）
```

### 2.3 错误分类学

```markdown
### 11.4 错误根因分类体系

每个Bug/问题在修复时，由Debug Agent标注根因类型（三选一）：

| 一级分类 | 二级分类 | 示例 | 标记 |
|---------|---------|------|------|
| **需求层面** | PRD歧义 | 同一术语两种理解 | `CAUSE:PRD_AMBIGUITY` |
| | PRD遗漏 | 边界情况未定义 | `CAUSE:PRD_OMISSION` |
| | PRD矛盾 | 两处描述冲突 | `CAUSE:PRD_CONFLICT` |
| **设计层面** | 契约设计不合理 | 接口粒度太粗/太细 | `CAUSE:CONTRACT_DESIGN` |
| | Schema设计缺陷 | 字段类型不匹配业务 | `CAUSE:SCHEMA_DESIGN` |
| | 模块边界错误 | 功能归属错模块 | `CAUSE:BOUNDARY_ERROR` |
| **实现层面** | 逻辑错误 | 条件判断写反 | `CAUSE:LOGIC_ERROR` |
| | 契约不一致 | 实现偏离contract | `CAUSE:CONTRACT_DRIFT` |
| | 测试不足 | 遗漏边界用例 | `CAUSE:TEST_GAP` |

**分类统计**（每个项目结束后自动汇总）：

```
需求层面问题：35%  ←── 如果持续>30%，Stage1的PRD质量门禁需要加强
设计层面问题：25%  ←── 如果持续>25%，↺拷问循环需要增加维度
实现层面问题：40%  ←── 如果持续>40%，Stage3的Code Review标准需要提高
```

> **根因反馈规则**：如果某一分类连续2个项目占比 > 阈值 → 触发该阶段的流程增强。
```

### 2.4 统计过程控制（SPC）

```markdown
### 11.5 项目运行健康趋势图

维护跨项目的运行趋势数据（`pipeline-trends.json`）：

```json
{
  "trends": [
    {
      "project": "iteration-01",
      "grill_first_pass": 0.50,
      "bug_escape_rate": 0.30,
      "p0_density": 2.1
    },
    {
      "project": "iteration-02", 
      "grill_first_pass": 0.75,
      "bug_escape_rate": 0.20,
      "p0_density": 1.5
    }
  ],
  "overall_trend": "IMPROVING"
}
```

**趋势判定规则**：
- 连续3个项目3个核心指标（grill首次通过率↑、Bug逃逸率↓、P0密度↓）同向改善 → `IMPROVING`
- 任一指标连续2个项目恶化 → `DEGRADING` → 触发根因分析
- 其他 → `STABLE`

> 趋势数据是方法论自身是否在进化的唯一量化证据。
```

### 2.5 门禁有效性审计

```markdown
### 11.6 门禁有效性审计

**问题**：门禁通过了≠门禁有效。需要事后验证每个门禁是否真的拦截了问题。

**审计方法**（每个项目结束后）：

| 门禁 | 审计问题 | 审计方式 |
|------|---------|---------|
| PRD MECE评审 | 有多少Stage4 Bug的根因是PRD问题？ | 统计 `CAUSE:PRD_*` 的Bug比例 |
| ↺拷问审查 | 有多少Stage4 Bug本应在grill阶段发现？ | 回溯每个Bug是否对应grill四维中的某一维 |
| Stage3 Code Review | 有多少Stage4 Bug本应在CR中发现？ | 回溯每个Bug是否违反api-contract或schema |
| Stage4集成测试 | 交付后有多少线上Bug？ | 如果有后续迭代，统计从用户反馈发现的Bug |

**审计结论**：
- 某门禁的"漏过率" > 阈值 → 门禁标准加严或增加检查项
- 某门禁连续3个项目漏过率 = 0 → 门禁可能过严（增加不必要的串行时间），考虑适度放宽
```

---

## 三、Round 2：建立学习闭环与知识沉淀（痛点R6-R8，让方法论"长记性"）

### 3.1 项目后回溯机制（Retro）

> 基于R1建立的量化指标，R2建立制度化回溯流程。

```markdown
## 十二、项目回溯与知识沉淀

### 12.1 回溯触发条件

| 触发条件 | 回溯级别 | 参与者 |
|---------|---------|--------|
| 每个项目Stage4完成后 | **标准回溯** (L1) | Coordinator自动执行 |
| 任一核心指标触发告警 | **深度回溯** (L2) | Coordinator + 人类 |
| 连续2个项目指标恶化 | **紧急回溯** (L3) | 全部角色Agent复盘 |

### 12.2 L1标准回溯（自动化）

Coordinator在Stage4交付后自动生成回溯报告（`retro-<project>.md`）：

```markdown
# 项目回溯报告 — iteration-NN

## 质量概览
- Grill首次通过率: 75% (✅ 达标)
- 首次CR通过率: 60% (⚠️ 低于目标70%)
- Bug逃逸率: 15% (✅ 达标)
- P0问题密度: 0.8/模块 (✅ 达标)

## 过程瓶颈识别
- 最长串行等待：Architect→Biz Expert = 5 min (正常)
- 最大并行度缺口：Stage3轮次2仅使用了2/3后端Slot (利用率67%)

## 根因分布
- 需求层面: 20% (2个bug)
- 设计层面: 30% (3个bug)  ← 需关注
- 实现层面: 50% (5个bug)

## 自动建议
1. 设计层面占比30% > 阈值25% → 建议下一项目↺循环增加"数据一致性"审查子维度
2. CR首次通过率60% < 目标70% → 建议强化Backend Agent的contract自检checklist
3. Stage3并行度67% → 考虑减少依赖链长度（合并小模块）
```

### 12.3 L2深度回溯（人类参与）

当自动建议涉及流程变更时（如新增grill维度、调整Agent上限），提交人类决策。

### 12.4 L3紧急回溯（全员复盘）

连续恶化触发时，暂停下一项目启动，进行：
1. 全流程走查上一个项目的完整执行日志
2. 识别恶化根因
3. 制定并实施流程修正
4. 修正后的流程在下个项目验证
```

### 3.2 方法论版本管理

```markdown
### 12.5 白皮书自身版本管理

**问题**：白皮书修改了，但36个配套Skill、各Agent的system prompt模板并未同步更新。

**方案**：白皮书作为唯一source of truth，所有下游产物引用其版本号。

白皮书文件头部：
```markdown
# all-in-mvp 多 Agent 并行开发流程 v2.3.0

> **版本历史**：
> - v2.3.0 (2026-05-21): 新增第十一章质量度量、第十二章项目回溯
> - v2.2.0 (2026-04-15): 增加③c测试用例静态审查
> - v2.1.0 (2026-03-10): Schema分表锁定机制
> - v2.0.0 (2026-02-01): 增加迭代模型与生命周期
> - v1.0.0 (2025-12-01): 初始发布
```

**下游同步机制**：
- 每个Skill和Agent prompt模板必须在头部声明 `@based_on: MVP白皮书 vX.Y.Z`
- Coordinator在Stage3启动时检查：所有Skill版本号 ≥ 白皮书大版本
- 版本不匹配 → WARNING，提示人类更新Skill

**修订触发规则**：
| 变更类型 | 版本号变化 | 示例 |
|---------|-----------|------|
| 新增章节/重大流程变更 | 大版本 (X+1.0.0) | 新增Stage5、改变并行度上限 |
| 新增子流程/新增门禁 | 次版本 (X.Y+1.0) | 新增③c审查、新增grill维度 |
| 措辞修正/示例补充 | 补丁版本 (X.Y.Z+1) | 修正描述、增加实例 |
```

### 3.3 对比基线建立

```markdown
### 12.6 方法论有效性对比基线

**问题**：声称并行开发更快更好，但无数据支撑。

**基线数据采集**（每个项目自动记录）：

| 对比维度 | 采集指标 | 对比参照 |
|---------|---------|---------|
| **时间效率** | Stage1→Stage4总耗时 | 估算单Agent串行开发耗时（模块数 × 单模块平均开发时间） |
| **质量指标** | Bug逃逸率、返工率 | 行业基线或无并行流程的历史数据 |
| **资源效率** | 总Agent-Hour / 功能点数 | 非并行项目的Agent-hour估算 |
| **并行增益** | 实际耗时 / 串行估算耗时 | 理想值 < 0.5（耗时减半） |

**采集方式**（不额外增加人工成本）：
- 总耗时：`pipeline-state.json` 中的首次spawn时间到Stage4门禁通过时间
- 串行估算：`task.md`中模块数 × 历史单模块平均TDD耗时
- Agent-Hour：每个Agent的实际活跃时间（从spawn到DONE）

> 基线数据不是用于宣传，而是用于验证方法论是否在"做正确的事"。
```

---

## 四、Round 3：建立自我进化机制（痛点R9-R10，让方法论成为"活的有机体"）

### 4.1 Agent能力画像与动态匹配

> 基于R1的量化指标和R2的回溯数据，R3建立Agent能力持续评估

```markdown
## 十三、Agent能力管理

### 13.1 Agent能力画像

每个项目结束后，Coordinator为每个Agent更新能力画像（`agent-profiles/<agent-name>.json`）：

```json
{
  "agent": "Backend-2",
  "role": "业务核心专家",
  "domain": ["产品", "类目", "订单", "库存"],
  "history": [
    {
      "project": "iteration-01",
      "modules_completed": 2,
      "p0_introduced": 1,
      "first_cr_pass": true,
      "avg_tdd_rounds": 4.0,
      "performance_score": 0.85
    }
  ],
  "aggregate": {
    "total_modules": 5,
    "avg_p0_per_module": 0.4,
    "first_cr_pass_rate": 0.8,
    "avg_tdd_rounds": 4.5,
    "overall_score": 0.82
  },
  "strengths": ["复杂业务逻辑实现", "TDD流程规范"],
  "weaknesses": ["异常路径覆盖不足", "接口契约自检遗漏"]
}
```

### 13.2 动态匹配权重调整

Coordinator的策略二（专家匹配）从**静态匹配**进化为**加权匹配**：

```
模块分配优先级 = 领域匹配度(40%) + 历史表现(30%) + 当前负载(20%) + 领域内连续分配次数(10%)
```

- 连续3个项目 `overall_score < 0.6` 的Agent → 降权，减少关键模块分配
- 连续3个项目 `overall_score > 0.9` 的Agent → 升权，优先分配复杂模块
- 权重每项目更新一次，不实时调整（避免震荡）

### 13.3 Agent数量动态建议

**问题**：白皮书硬编码后端3、前端3，但不同项目规模不同。

**自适应建议**（Coordinator在Stage3启动时计算）：

```
建议后端Agent数 = MIN(3, CEIL(模块总数 / 3))
建议前端Agent数 = MIN(3, CEIL(路由模块数 / 3))
```

- 模块数 ≤ 3 → 建议1个Agent（避免slot浪费）
- 模块数 4-6 → 建议2个Agent
- 模块数 ≥ 7 → 建议3个Agent（上限）
```

### 4.2 渐进式质量门禁收紧

> 让质量门禁随项目成熟度自动提高

```markdown
### 13.4 渐进式质量门禁

**原则**：门禁标准不应一成不变。项目越成熟，门槛越高。

| 项目代际 | Grill首次通过率 | 首次CR通过率 | P0密度上限 | Bug逃逸率上限 | 
|---------|---------------|-------------|-----------|-------------|
| 第1-2个项目（探索期） | ≥ 50% | ≥ 50% | ≤ 3/模块 | ≤ 30% |
| 第3-5个项目（稳定期） | ≥ 75% | ≥ 70% | ≤ 1/模块 | ≤ 20% |
| 第6+个项目（成熟期） | ≥ 90% | ≥ 85% | ≤ 0.5/模块 | ≤ 10% |

**收紧触发**：连续2个项目达到当前代际所有指标目标 → 自动升级到下一代的指标标准。

**不达标的降级**：连续2个项目达不到当前代际标准 → 不降低标准（避免"将就"），而是触发L3紧急回溯。

> 这确保了方法论的"肌肉"随训练而增强——而非永远停留在入门水平。
```

### 4.3 Grill审查维度自适应扩充

```markdown
### 13.5 Grill审查维度自适应机制

**问题**：当前grill只有四维审查（需求覆盖、模块边界、术语一致、验收标准对齐）。但不同项目的缺陷模式不同。

**自适应扩充规则**：

1. 每次L2/L3回溯中，如果某一"非四维"的问题类型占比 > 15%且连续2个项目出现：
   → 自动提议新增grill审查维度

**实例**：

| 新增维度候选 | 触发条件 | 审查内容 |
|------------|---------|---------|
| 数据一致性 | 连续2个项目出现≥3个"数据不一致"类Bug | 检查schema定义与PRD数据规则的一致性 |
| 权限完整性 | 连续2个项目出现≥2个"权限越界"类Bug | 检查api-contract中每个端点的权限标注是否完整 |
| 并发安全性 | 连续2个项目出现≥2个"竞态条件"类Bug | 检查并发场景是否在PRD中有定义、在spec中有应对 |
| 前端状态同步 | 连续2个项目出现≥3个"前端与后端状态不同步"类Bug | 检查Mock的异常path是否覆盖PRD中的多状态规则 |

**新增维度的生命周期**：
- 提议阶段：人类批准
- 试行阶段：在下一项目中作为WARNING级别（不阻塞）
- 正式阶段：试行1个项目后评估 → 升级为ERROR级别（阻塞）
- 退役阶段：连续3个项目该维度无发现 → 降低为WARNING或移除
```

### 4.4 方法论分叉实验机制

```markdown
### 13.6 A/B实验框架（可选，成熟期启用）

**前提**：项目进入成熟期（6+代际），有充足的数据基线。

**实验设计**：
- 在非关键模块上，交替使用两套略有差异的流程（如grill的严格度不同、CR时机不同）
- 对比两组的质量指标
- 统计显著（p<0.05）的差异 → 采纳更优方案并入白皮书

**实验范围限制**：
- 只能是Stage2/Stage3的非核心流程调整
- 不能影响Stage门禁的完整性
- 每次只变一个变量
- 实验前声明假设、样本量、判定标准

> 这是方法论从"经验驱动"走向"数据驱动"的最后一步。
```

### 4.5 知识库自动沉淀

```markdown
### 13.7 跨项目模式识别与自动建议

Coordinator维护一个跨项目的"模式库"（`pipeline-patterns.json`）：

```json
{
  "patterns": [
    {
      "id": "P001",
      "pattern": "模块数>8 → Grill收敛>2轮",
      "evidence_count": 3,
      "affected_projects": ["iter-01", "iter-05", "iter-08"],
      "auto_suggestion": "模块数超过8时，建议业务专家在task.md中增加模块分组（grouping），让grill按组审查而非逐模块审查"
    },
    {
      "id": "P002", 
      "pattern": "认证模块先行 → 后续模块CR首次通过率提升20%",
      "evidence_count": 4,
      "auto_suggestion": "考虑将认证模块设为Stage3第0轮强制先行（即使无显式依赖）"
    }
  ]
}
```

**模式发现**：Coordinator在每次L1回溯时自动对比当前项目与历史模式库，新增或加强已有模式。
**模式应用**：下一项目Stage2启动时，Coordinator读取模式库，将匹配的auto_suggestion注入对应的Agent prompt。
```

---

## 五、三轮迭代效果对比

| 维度 | 现状（v1.0） | Round 1 | Round 2 | Round 3 |
|------|-----------|---------|---------|---------|
| **量化能力** | ❌ 全二元判断 | ✅ 12项KQI+指标仪表盘 | — | ✅ |
| **Grill标定** | ❌ 无标定 | ✅ 延迟验证+假阴性/精准度标定 | — | ✅ |
| **错误分类** | ❌ 无分类 | ✅ 3大类9子类根因分类 | — | ✅ |
| **统计过程控制** | ❌ 无 | ✅ 跨项目趋势监测 | — | ✅ |
| **门禁审计** | ❌ 无 | ✅ 门禁漏过率追溯 | — | ✅ |
| **项目回溯** | ❌ 无 | — | ✅ L1/L2/L3三级回溯 | ✅ |
| **方法论版本管理** | ❌ 无版本号 | — | ✅ Semver+下游同步 | ✅ |
| **对比基线** | ❌ 无数据 | — | ✅ 并行增益量化 | ✅ |
| **Agent能力画像** | ❌ 无 | — | — | ✅ 动态权重+自适应数量 |
| **渐进式门禁** | ❌ 静态标准 | — | — | ✅ 三阶段自动收紧 |
| **Grill维度进化** | ❌ 固定四维 | — | — | ✅ 数据驱动扩充/退役 |
| **A/B实验框架** | ❌ 无 | — | — | ✅ 成熟期可选 |
| **模式识别** | ❌ 无 | — | — | ✅ 跨项目自动模式建议 |

---

## 六、三层严谨性递进关系

```
Round 1: 让流程"看得见"
    问题 → 数据 → 度量 → 可视化
    从"感觉不错"到"数据显示"

Round 2: 让流程"长记性"  
    数据 → 回溯 → 分析 → 沉淀 → 反哺
    从"每次从零开始"到"经验可遗传"

Round 3: 让流程"会进化"
    模式 → 假设 → 实验 → 验证 → 固化
    从"静态方法论"到"活的有机体"
```

**核心哲学**：严谨性不是"检查项多"，而是"知道哪里不够严谨并且有机制让它变得更严谨"。

---

## 七、实施路线图

| 阶段 | 实施内容 | 前置条件 | 预计收益 |
|------|---------|---------|---------|
| **即刻** | R1-2.1 质量指标仪表盘 + pipeline-metrics.json | 无 | 首次获得量化视角 |
| **即刻** | R1-2.3 错误根因分类（在Debug Agent prompt中追加） | 无 | 从修Bug到修Bug的根 |
| **1周内** | R2-3.1 L1标准回溯自动化 | R1指标就绪 | 每个项目自动产生改进建议 |
| **1周内** | R2-3.2 白皮书版本管理 | 无 | 下游同步不再混乱 |
| **2周内** | R1-2.2 Grill有效性标定 | 完成2个项目 | 拷问者第一次被拷问 |
| **2周内** | R1-2.5 门禁有效性审计 | R1指标就绪 | 门禁不再是稻草人 |
| **1个月内** | R3-4.1 Agent能力画像 | 完成3个项目 | 匹配不再是拍脑袋 |
| **1个月内** | R3-4.2 渐进式门禁 | 完成5个项目 | 标准随项目成长 |
| **成熟期** | R3-4.3 Grill维度自适应 | 完成6+项目 | 审查维度从固定到生长 |
| **成熟期** | R3-4.4 A/B实验框架 | 数据基线稳定 | 方法论走向科学 |

---

*严谨不是终点，是持续逼近的向量。这三轮迭代将白皮书从"一份好文档"升级为"一个能自我改进的系统"。*
